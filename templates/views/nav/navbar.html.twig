<nav class="grid grid-cols-3 items-center px-4 text-white h-16">
    <div class="flex justify-start">
        <h1 class="font-bold">Quest.ENI</h1>
    </div>

    <div class="flex justify-center items-center gap-x-6">
        <a href="{{ path('quest_index') }}" class="hover:text-yellow-500 transition">display quests</a>

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('user_index') }}" class="hover:text-yellow-500 transition">display user</a>
            <a href="{{ path('user_create') }}" class="hover:text-yellow-500 transition">add User</a>
        {% endif %}

        <a href="{{ path('main_home') }}" class="text-[1.5rem] font-bold text-yellow-500">Home</a>

        <a href="{{ path('quest_create') }}" class="hover:text-yellow-500 transition">create quests</a>

        {% if app.user %}
            <a href="{{ path('user_profile') }}" class="hover:text-yellow-500 transition">Profile</a>
        {% endif %}
    </div>

    <div class="flex justify-end">
        {% if app.user %}
            <a href="{{ path('app_logout') }}" class="hover:underline text-gray-300">logout</a>
        {% else %}
            <a href="{{ path('app_login') }}" class="hover:text-yellow-500 transition">login</a>
        {% endif %}
    </div>
</nav>

<div class="border-b-2 border-yellow-600"></div>
